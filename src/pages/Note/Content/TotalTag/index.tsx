import React, { FC, ReactElement } from 'react';
import './index.scss'

const TotalTag:FC = ():ReactElement => {
  return (
    <div id="note-content-totaltag">
      TotalTag
    </div>
  );
}

export default TotalTag;
